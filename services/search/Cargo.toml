[package]
name = "paystream-search"
version = "0.1.0"
edition = "2021"
description = "Stream search and filter service for PayStream"
license = "Apache-2.0"

[dependencies]
serde = { version = "1.0.203", features = ["derive"] }
